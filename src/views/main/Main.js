import Header from '../../components/header';
import MainLinks from '../../components/mainLinks';

const Main = () => {
    return (
        <div>
            <Header title={'Main'} />
            <MainLinks />
        </div>
    )
}

export default Main;