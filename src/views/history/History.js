import Header from '../../components/header';
import Shortcut from '../../components/History/shortcut/Shortcut';

const History = () => {
    return (
        <div>
            <Header title={'Historia'} />
            <Shortcut />
        </div>
    )
}

export default History;